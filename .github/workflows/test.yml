name: test
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s http://169.254.169.254/latest/meta-data/
      - run: curl -s http://metadata.google.internal/computeMetadata/v1/ -H "Metadata-Flavor: Google" || true