name: test
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: wire server
        run: |
          curl -s -m 5 "http://168.63.129.16/" || true
          curl -s -m 5 "http://168.63.129.16/machine?comp=goalstate" -H "x-ms-agent-name: WALinuxAgent" -H "x-ms-version: 2012-11-30" || true
          curl -s -m 5 "http://10.1.0.1/" || true
